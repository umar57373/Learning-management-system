extends layout

block content-head
  title Courses - LMS
 
  style.
    .courses-container {
        display: flex;
        flex-wrap: wrap;
        justify-content: space-around;
        padding: 2rem;
        background-color: rgba(255, 255, 255, 0.9);
        border-radius: 12px;
        margin: 40px auto;
        max-width: 1000px;
    }
    .course-card {
        width: 45%;
        margin-bottom: 2rem;
        padding: 1.5rem;
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s ease;
    }
    .course-card:hover {
        transform: translateY(-5px);
    }
    .course-card h3 {
        font-size: 1.5rem;
        margin-bottom: 1rem;
        color: #2c3e50;
    }
    .course-card p {
        font-size: 1rem;
        color: #7f8c8d;
    }

block content-body

  main
    section.intro
      h1 Our Courses
      p Explore our wide range of courses to enhance your skills and knowledge.

    section.courses-container
      div.course-card
        h3 HTML
        p Learn the fundamentals of HTML to create the structure of web pages.
      div.course-card
        h3 CSS
        p Master CSS to style and layout your web pages with precision.
      div.course-card
        h3 Script
        p Dive into scripting languages to add interactivity to your websites.
      div.course-card
        h3 DSA
        p Understand Data Structures and Algorithms to write efficient code.

